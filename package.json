{
  "name": "123soleil",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "cd frontend && npm run dev",
    "build": "cd frontend && npm run build",
    "start": "cd frontend && npm run start",
    "lint": "cd frontend && npm run lint",
    "directus:setup": "node backend/setup.js",
    "directus:migrate": "node backend/migrate.js",
    "directus:schema:export": "node backend/export-schema.js",
    "directus:schema:apply": "node backend/apply-schema.js",
    "deploy": "docker compose up -d --build && echo 'Attente Directus (25s)...' && sleep 25 && npm run directus:schema:apply"
  },
  "dependencies": {
    "dotenv": "^17.2.3",
    "gray-matter": "^4.0.3"
  }
}
